# Bugfix Orchestrator v2 实施计划

> 面向 AI 开发人员：本文件是 v2 的执行说明，不替代 [需求文档](/Users/sunyi/ai/MySkills/memory-bank/features/v1/需求文档.md)、[技术方案](/Users/sunyi/ai/MySkills/memory-bank/features/v1/技术方案.md)、[验收文档](/Users/sunyi/ai/MySkills/memory-bank/features/v1/验收文档.md) 或 [AGENTS.md](/Users/sunyi/ai/MySkills/AGENTS.md)。实施时必须先遵守上位文档和用户本轮指令，再按本计划逐步推进。

## 1. 文档定位

### 1.1 目标

本文件用于把 v2 要实现的能力拆解为可直接执行的实施步骤，供 AI 开发人员按顺序落地。文档只写清晰、具体、可验证的说明，不包含代码、伪代码或命令输出样例。

### 1.2 适用对象

- 负责实现 `Bugfix Orchestrator Agent + Skills + CLI` 的 AI 开发人员
- 负责审查 v2 优先级、测试覆盖和交付边界的技术负责人

### 1.3 信息优先级

1. 用户当轮明确指令
2. [需求文档](/Users/sunyi/ai/MySkills/memory-bank/features/v1/需求文档.md)
3. [技术方案](/Users/sunyi/ai/MySkills/memory-bank/features/v1/技术方案.md)
4. [验收文档](/Users/sunyi/ai/MySkills/memory-bank/features/v1/验收文档.md)
5. 本文件
6. [AGENTS.md](/Users/sunyi/ai/MySkills/AGENTS.md)

补充执行裁决规则：

- 本计划同时承接“v1 文档中已要求但尚未实现的缺口”与“用户单独授权的新增能力”。
- 若本计划未写明具体实现口径，默认先按技术方案收敛，再回看需求文档与验收文档。
- 若技术方案、验收文档与用户最新要求之间存在无法局部解释的冲突，必须升级给用户决定，不能自行猜测。

### 1.4 v2 范围

本计划覆盖两层内容，并按优先级顺序执行：

- 第一优先级：实现“Jira 子任务与 GitLab 开发分支绑定、fix commit 显式关联”
- 第二优先级：补齐 v1 需求文档中仍未形成真实用户能力的缺口

本计划中的 v2 目标至少包括：

- 支持项目画像配置 Jira 子任务创建、branch 绑定、commit 绑定规则
- 支持开发者手工建分支后，通过 CLI 显式绑定 bug Jira 与当前开发分支
- 支持预览并执行 Jira 开发子任务创建
- 支持显式补录 fix commit，并关联到指定 bug Jira / 子任务 Jira
- 补齐项目画像加载、Jira 真实读取、主流程自动编排、Requirement Brief 导出、真实 preview / execute、Bugfix Report 导出等 v1 缺口

### 1.5 v2 非目标

本计划不得擅自扩展以下能力：

- 自动创建本地 Git 分支
- 自动创建 commit、MR 或自动合并代码
- 自动修改代码
- 自动执行测试
- 服务端多用户调度
- 自然语言对话式入口
- 超出当前文档授权的无人值守自治修复能力

## 2. 全局执行规则

### 2.1 基本原则

- 每次只推进一个最小能力闭环，不把多个未验证改动堆在一起。
- 必须先补测试或先写验证说明，再做实现。
- 每完成一个任务组就立即完成对应验证，验证未通过不得进入下一任务组。
- 所有关键结论都要能追溯到需求文档、技术方案、验收文档或用户指令。
- 影响术语、阶段、状态、对象、目录或职责的改动，必须同步检查相关文档。

### 2.2 分层约束

- `CLI Layer` 只负责命令入口、参数校验、交互和展示，不持有业务状态。
- `Workflow/Agent Layer` 是唯一业务状态 owner，唯一负责阶段流转、审批、回退、恢复和 checkpoint。
- `Skill Layer` 只接受结构化输入并返回结构化输出，不直接做 I/O，不持久化 run state。
- `Infrastructure Layer` 是唯一外部系统与本地仓库访问入口，负责读取、preview、execute、错误标准化。
- `Renderers` 只负责 CLI、Markdown、JSON 渲染，不参与业务推理。

### 2.3 风险约束

- 所有外部写操作都必须遵守 `preview -> approve -> execute`。
- Jira 子任务创建、branch 绑定、commit 绑定都属于真实外部写操作，必须走统一 preview / execute 链路。
- 所有恢复动作都必须基于已持久化 checkpoint，不能依赖内存态。
- 所有真实外部写入都必须有幂等依据、账本记录和对账规则。
- 遇到 `outcome_unknown` 或存在 `prepared` / `dispatched` 但无终态的副作用记录时，恢复入口必须先进入 reconcile，禁止直接重发外部写入。
- 敏感字段不得明文落盘，不得出现在普通日志和导出文档中。
- 未在上位文档或用户指令中授权的能力，不得以“顺手补齐”为理由加入实现。

## 3. 跨任务契约

下列接口与类型视为跨任务契约，任何任务不得私自改名、弱化语义或改变 owner：

- CLI 命令组：`bind`、`inspect`、`run`、`record`
- 新 CLI 动作：`run bind-branch`、`run ensure-subtask`、`run provide-fix-commit`
- 主流程阶段：`Intake`、`Context Resolution`、`Requirement Synthesis`、`Code Localization`、`Fix Planning`、`Execution`、`Artifact Linking`、`Knowledge Recording`
- 阶段状态：`not_started`、`in_progress`、`output_ready`、`waiting_approval`、`approved_pending_write`、`executing_side_effect`、`waiting_external_input`、`completed`、`failed`、`stale`、`skipped`
- 审批决定：`approve`、`reject`、`revise`
- 审批状态：`none`、`pending`、`approved`、`rejected`、`revise_requested`、`superseded`
- run 状态：`run_lifecycle_status` 与 `run_outcome_status` 双轨
- 核心对象：`ProjectProfile`、`ExecutionContext`、`RequirementBrief`、`BugfixReport`、`ApprovalRecord`、`SideEffectLedgerEntry`、`CheckpointRecord`、`StructuredError`
- 新上下文字段：`active_bug_issue_key`、`jira_subtask_ref`、`jira_subtask_result_ref`、`git_branch_binding_ref`、`git_commit_binding_refs`
- 新副作用 operation：`jira.create_subtask`、`jira.bind_branch`、`jira.bind_commit`
- Skill 契约：显式输入 schema、结构化输出、统一错误对象、无状态、不得直接做 I/O
- Connector 能力模型：读取、目标解析、preview、execute、artifact 解析、repo 访问都只能经由 infrastructure 层

## 4. 统一步骤模板

v2 的每个任务组都必须沿用以下固定模板：

- `目标`
- `前置条件`
- `建议落点`
- `步骤`
- `测试/验证`
- `完成标准`

其中：

- 步骤只描述一组最小可验证动作，不堆叠无验证的大改。
- 测试/验证必须至少写明：验证对象、触发方式、预期结果、通过标准。
- 完成标准必须体现“该任务组完成后，后续任务可以安全接着做什么”。

## 5. v2 任务拆解

### 任务 1：扩展项目画像与配置检查，支持 Jira 子任务 / branch / commit 绑定规则

**目标：** 让 `ProjectProfile` 能表达 Jira 子任务创建规则、branch 绑定规则、commit 绑定规则，并通过 `bind jira`、`bind gitlab`、`inspect config` 暴露给用户。

**前置条件：**

- 已阅读需求文档中的“项目关系绑定需求”“Jira 集成需求”“GitLab 集成需求”
- 已阅读技术方案中的 `ProjectProfile`、`config-loader`、CLI `bind` / `inspect` 设计
- 已确认本轮新增能力为 v2 第一优先级

**建议落点：**

- `src/domain/`
- `src/skills/config-loader/`
- `src/app/project-profile.ts`
- `src/cli/bind/`
- `src/cli/inspect/`
- `tests/unit/`
- `tests/integration/cli/`

**步骤：**

1. 在 `ProjectProfile.jira` 中补齐子任务创建、branch 绑定、commit 绑定的最小配置字段，并明确必填与选填边界。
2. 在 `ProjectProfile.gitlab` 中补齐 branch 绑定输入模式等最小字段，并保持与既有 `branch_naming_rule` 兼容。
3. 扩展 `bind jira`、`bind gitlab` 的配置写入与 schema 校验逻辑。
4. 扩展 `inspect config` 的完整性检查与缺失项提示逻辑。

测试/验证：

- 验证对象：新增项目画像字段与配置检查链路
- 触发方式：覆盖完整配置、缺失字段、非法字段、旧配置兼容四类场景
- 预期结果：配置缺失时返回结构化错误；`inspect config` 能指出新增字段缺口
- 通过标准：本任务只完成 schema、配置读写、校验和检查，不混入真实 Jira 写回

**完成标准：**

- 项目画像可承载 Jira 子任务、branch、commit 绑定规则
- `bind` / `inspect` 能识别新增字段
- 后续任务可直接复用稳定配置来源

### 任务 2：扩展核心数据模型、artifact 与 side-effect ledger 契约

**目标：** 为 branch 绑定、子任务 preview / result、commit 绑定补齐最小对象、ref 和副作用账本契约。

**前置条件：**

- 已完成任务 1
- 已阅读需求文档中的“执行上下文需求”“Jira 写回契约要求”“GitLab 产物契约要求”
- 已阅读技术方案中的 `ExecutionContext`、`SideEffectLedgerEntry`、`CheckpointRecord`

**建议落点：**

- `src/domain/`
- `src/workflow/`
- `tests/domain/`
- `tests/unit/`

**步骤：**

1. 为 `ExecutionContext` 增加 `active_bug_issue_key`、`jira_subtask_ref`、`jira_subtask_result_ref`、`git_branch_binding_ref`、`git_commit_binding_refs` 等最小字段。
2. 为 branch 绑定、子任务 preview / result、commit 绑定补齐 artifact ref 约定。
3. 固定 `jira.create_subtask`、`jira.bind_branch`、`jira.bind_commit` 的 side-effect operation 命名与最小账本字段。
4. 明确这些新增字段在 `context.json`、artifact、ledger 之间的承载边界。

测试/验证：

- 验证对象：新增上下文、artifact 与账本契约
- 触发方式：逐项审查 schema 字段、artifact ref、ledger operation 和持久化边界
- 预期结果：新增字段只保存 key、ref、hash 和摘要；完整 Jira payload 只进入 artifact
- 通过标准：不存在把完整 Jira 请求体或响应体直接塞入 `context.json` 的设计

**完成标准：**

- v2 新能力所需核心对象和 operation 已冻结
- 后续 CLI、workflow、connector 可基于统一契约实现
- 新能力不会破坏现有存储边界和恢复语义

### 任务 3：新增 CLI 与 workflow 入口，支持 branch 绑定、子任务确保、fix commit 补录

**目标：** 为用户新增可直接使用的命令入口，承接 branch 绑定、子任务创建前置动作和 fix commit 显式补录。

**前置条件：**

- 已完成任务 2
- 已阅读需求文档中的“CLI 需求”“工作流需求”“子工作流要求”
- 已阅读技术方案中的 CLI 分组与 workflow 阶段设计

**建议落点：**

- `src/cli/run/`
- `src/app/cli-orchestration.ts`
- `src/workflow/`
- `tests/integration/cli/`
- `tests/unit/workflow/`

**步骤：**

1. 新增 `run bind-branch`，用于显式绑定 bug Jira 与当前开发分支。
2. 新增 `run ensure-subtask`，用于创建或确认该 bug 对应的开发子任务。
3. 新增 `run provide-fix-commit`，用于补录 fix commit 并声明归属 issue。
4. 把这三个入口接到统一 workflow 状态语义中：branch 未提供时 `Execution` 可进入 `waiting_external_input`；子任务创建前必须先 preview 和审批；branch / commit 关联写回落在 `Artifact Linking`。

测试/验证：

- 验证对象：新 CLI 命令入口与 workflow 路由
- 触发方式：覆盖 branch 已提供、branch 缺失、ensure-subtask 首次执行、fix commit 补录、非法参数五类场景
- 预期结果：三条命令有稳定输入输出；状态机能表达等待、审批和补录后的下一步动作
- 通过标准：不存在绕过 preview / approve / execute 的旁路

**完成标准：**

- 用户可通过 CLI 显式录入 branch 和 fix commit
- workflow 能稳定承接这些新输入
- 后续 connector 与真实写回可以直接挂接到这些入口

### 任务 4：扩展 Jira connector，支持创建子任务与 branch / commit 关联

**目标：** 为 Jira connector 增加创建子任务、绑定 branch、绑定 commit 的 preview、execute、dedupe 与恢复契约。

**前置条件：**

- 已完成任务 3
- 已阅读需求文档中的“Jira 集成需求”“dry-run 与真实写入验收”
- 已阅读技术方案中的 Jira connector、副作用账本、preview / execute 契约

**建议落点：**

- `src/infrastructure/connectors/jira/`
- `src/workflow/jira-writeback.ts`
- `tests/unit/jira-writeback/`
- `tests/integration/`

**步骤：**

1. 为 `jira.create_subtask` 定义 preview draft、execute result 和 dedupe 查询语义。
2. 为 `jira.bind_branch`、`jira.bind_commit` 定义 preview draft、execute result 和目标解析语义。
3. 固定三类操作的 `prepared`、`dispatched`、`succeeded`、`failed`、`outcome_unknown` 账本顺序。
4. 明确 dry-run 与真实 execute 共享同一 payload 基线，恢复时先 reconcile 后决定是否重试。

测试/验证：

- 验证对象：Jira 子任务创建、branch 绑定、commit 绑定 connector 契约
- 触发方式：覆盖 dry-run preview、真实 execute、重复执行、`outcome_unknown` 恢复四类场景
- 预期结果：同一个 bug + branch 重复执行可 dedupe；恢复先 reconcile，禁止重复创建子任务或重复写回
- 通过标准：preview 与 execute 的差异只允许出现在动态结果字段，而不是请求语义本身

**完成标准：**

- Jira connector 已具备 v2 新能力所需的最小真实写回契约
- branch / commit / subtask 三条链路都能被 preview、审批、账本和恢复覆盖
- 第一优先级新增能力的基础设施链路打通

### 任务 5：补齐项目画像与主流程接线

**目标：** 让 `run start`、`run brief`、`record jira`、`record feishu` 在进入 workflow 前强制加载并校验项目画像，拒绝空壳 run。

**前置条件：**

- 已完成任务 4
- 已阅读验收文档中的“`run` / `record` 未实际加载并校验项目画像”
- 已阅读技术方案中的 `config-loader`、`src/app`、CLI 装配层职责

**建议落点：**

- `src/app/`
- `src/skills/config-loader/`
- `src/cli/`
- `tests/integration/cli/`

**步骤：**

1. 在进入 `run start`、`run brief`、`record jira`、`record feishu` 前统一加载项目画像。
2. 将配置缺失、非法引用、版本不兼容等错误映射为结构化 CLI 错误。
3. 阻止未绑定项目的命令继续创建 run。
4. 保持既有 `bind` / `inspect` 边界不变。

测试/验证：

- 验证对象：项目画像与主流程接线
- 触发方式：覆盖完整配置、缺失配置、配置非法、配置修复后重试四类场景
- 预期结果：配置缺失直接失败，不允许初始化空壳 run
- 通过标准：项目画像成为主流程唯一可信配置来源

**完成标准：**

- `run` / `record` 不再绕过项目画像
- 配置错误在进入 workflow 前被识别
- 后续主流程自动编排可建立在可信配置之上

### 任务 6：补齐 Jira 真实读取

**目标：** 根据 `issue_key` 真实读取 Jira bug 快照，并将读取结果作为 `Intake` 阶段唯一事实源。

**前置条件：**

- 已完成任务 5
- 已阅读验收文档中的“未根据 `issue_key` 真实拉取 Jira bug 信息”
- 已阅读需求文档中的“Jira 集成需求”
- 已阅读技术方案中的 `jira-intake`、Jira connector

**建议落点：**

- `src/infrastructure/connectors/jira/`
- `src/skills/jira-intake/`
- `src/app/`
- `tests/integration/`
- `tests/unit/intake/`

**步骤：**

1. 打通 Jira bug 读取入口，获取 issue key、状态、描述、标签、关联线索和写回目标所需字段。
2. 把读取结果落为 Jira snapshot artifact。
3. 让 `jira-intake` 只消费结构化快照，而不是自己访问原始接口。
4. 统一权限不足、字段缺失、找不到 issue、网络错误的错误语义。

测试/验证：

- 验证对象：Jira 真实读取与 `Intake` 阶段输入
- 触发方式：覆盖合法 issue、权限不足、issue 不存在、字段缺失四类场景
- 预期结果：读取结果进入 `Intake` artifact 与 `ExecutionContext`
- 通过标准：Jira snapshot 成为后续阶段唯一事实源

**完成标准：**

- `issue_key` 已能触发真实读取
- `jira-intake` 与 connector 边界清晰
- 主流程已具备真实启动输入

### 任务 7：补齐主流程自动编排

**目标：** 让 `run start` 从 issue key 出发，真实走通前五个分析阶段，并在关键节点进入审批门。

**前置条件：**

- 已完成任务 6
- 已阅读验收文档中的“`run start` 未串起标准主流程”
- 已阅读需求文档中的“工作流需求”“审批与人工确认需求”
- 已阅读技术方案中的 workflow 设计

**建议落点：**

- `src/app/`
- `src/workflow/`
- `src/skills/project-context/`
- `src/skills/requirement-summarizer/`
- `src/skills/code-locator/`
- `src/skills/fix-planner/`
- `tests/integration/`
- `tests/acceptance/`

**步骤：**

1. 在 `run start` 中依次编排 `Intake`、`Context Resolution`、`Requirement Synthesis`、`Code Localization`、`Fix Planning`。
2. 为每个阶段产出结构化 artifact、checkpoint 和状态推进。
3. 在 `Requirement Synthesis` 和 `Fix Planning` 后进入真实审批门。
4. 保持 `revise`、`stale`、恢复和重复执行语义稳定。

测试/验证：

- 验证对象：主流程自动编排
- 触发方式：覆盖标准启动、审批通过、审批退回、恢复继续四类场景
- 预期结果：`run start` 能真实走通前五阶段，并在两个审批点停住或推进
- 通过标准：阶段结果、artifact 和 checkpoint 变化可被 CLI 与测试观察到

**完成标准：**

- 主流程分析阶段打通
- 审批链进入真实可用状态
- 后续 Requirement Brief 导出和写回链路有稳定上游输入

### 任务 8：补齐 Requirement Brief 对外交付

**目标：** 让 `run brief` 真实生成 `Requirement Brief`，并支持 CLI 展示与 Markdown 导出。

**前置条件：**

- 已完成任务 7
- 已阅读验收文档中的“`run brief` 未真正生成并导出 `Requirement Brief`”
- 已阅读需求文档中的“需求文档生成需求”“子工作流要求”
- 已阅读技术方案中的 `RequirementBrief`、renderers 设计

**建议落点：**

- `src/skills/requirement-summarizer/`
- `src/renderers/`
- `src/cli/run/`
- `tests/integration/`
- `tests/requirement-brief/`

**步骤：**

1. 打通 `brief_only` 子工作流，使其真正生成 `Requirement Brief`。
2. 为 CLI 输出补齐 brief 最小字段展示。
3. 为 `--output` 或等价导出入口补齐 Markdown 导出。
4. 保持 unresolved requirement 场景的显式标记与原因说明。

测试/验证：

- 验证对象：`Requirement Brief` 生成与导出
- 触发方式：覆盖已绑定需求、未绑定需求、CLI 展示、Markdown 导出四类场景
- 预期结果：`run brief` 能生成并导出 `Requirement Brief`
- 通过标准：CLI 与 Markdown 在业务信息上保持一致，不依赖手工拼装

**完成标准：**

- `Requirement Brief` 已形成真实对外交付能力
- `run brief` 不再只是创建空的子工作流 run
- brief 可被主流程、子工作流和后续报告引用

### 任务 9：补齐代码定位与修复计划主流程接线

**目标：** 将 `project-context`、`requirement-summarizer`、`code-locator`、`fix-planner` 全部接入主流程，并让每阶段都落 artifact、checkpoint、审批引用。

**前置条件：**

- 已完成任务 8
- 已阅读验收文档中的“未将 `project-context`、`requirement-summarizer`、`code-locator`、`fix-planner` 接到主流程”
- 已阅读需求文档中的“Code Localization”“Fix Planning”
- 已阅读技术方案中的相关 skill 设计

**建议落点：**

- `src/skills/project-context/`
- `src/skills/requirement-summarizer/`
- `src/skills/code-locator/`
- `src/skills/fix-planner/`
- `src/workflow/`
- `tests/unit/`
- `tests/integration/`

**步骤：**

1. 补齐 `Context Resolution` 阶段对项目、仓库、需求、模块解析结果的真实落地。
2. 补齐 `Code Localization` 阶段的候选代码位置和影响模块产物。
3. 补齐 `Fix Planning` 阶段的修复建议、验证建议和开放风险产物。
4. 确保这些产物都进入 artifact、checkpoint 与审批引用链路。

测试/验证：

- 验证对象：代码定位与修复计划主流程接线
- 触发方式：覆盖唯一命中、多候选、无结果、fix plan 审批四类场景
- 预期结果：四个 skill 全部接入主流程，阶段产物都可被恢复和回退引用
- 通过标准：不再依赖内部实现或手工推断作为主流程产物来源

**完成标准：**

- 代码定位和修复计划已成为主流程真实阶段
- 每阶段都有结构化 artifact 和状态推进
- 主流程分析链路在 v1 口径下完整可用

### 任务 10：补齐子工作流手工补录与 requirement mapping 人工补录

**目标：** 统一补齐 `record jira`、`record feishu` 的最小前置输入能力，以及 requirement manual override / selection 的 CLI 入口。

**前置条件：**

- 已完成任务 9
- 已阅读验收文档中的“未提供 requirement mapping 的人工补录 CLI 入口”“`record` 只创建子工作流 run”
- 已阅读需求文档中的“场景 4”“场景 5”“子工作流要求”
- 已阅读技术方案中的子工作流和等待外部输入设计

**建议落点：**

- `src/cli/record/`
- `src/cli/run/`
- `src/app/`
- `src/workflow/`
- `tests/integration/`
- `tests/acceptance/`

**步骤：**

1. 为 `record jira`、`record feishu` 补齐最小前置输入的显式补录能力。
2. 为 requirement mapping 歧义或失败场景新增 manual override / selection CLI 入口。
3. 让补录完成后可从最近 checkpoint 继续，而不是重建整个 run。
4. 确保子工作流不绕过审批门与 preview 机制。

测试/验证：

- 验证对象：子工作流手工补录与 requirement mapping 人工补录
- 触发方式：覆盖仅回写 Jira、仅写入飞书、唯一需求命中失败、多候选需求确认四类场景
- 预期结果：`record jira` / `record feishu` 能基于最小输入独立跑通；requirement mapping 歧义可人工补录并从 checkpoint 继续
- 通过标准：补录不是旁路文件修改，且不会绕过既有审批 / 恢复语义

**完成标准：**

- 子工作流具备真实可用的手工补录能力
- requirement mapping 歧义场景被正式纳入 CLI 闭环
- 主流程和子工作流的恢复体验一致

### 任务 11：补齐真实写回、报告导出与缺失 skill 契约

**目标：** 收尾补齐 Jira / 飞书真实 preview 与 execute、`Bugfix Report` 导出，以及 `feishu-recorder`、`approval-gate`、`connector-router`、`artifact-renderer` 的实现或正式命名收敛。

**前置条件：**

- 已完成任务 10
- 已阅读验收文档中的“`preview-write` / `execute-write` 未输出真实 payload / 未执行真实写入”“未提供 `Bugfix Report` 导出命令”“缺失 skill 未形成对应实现”
- 已阅读需求文档中的“Jira 集成需求”“飞书集成需求”“输出与报告需求”“Skill 需求”
- 已阅读技术方案中的 writeback、report 与 skill 分层设计

**建议落点：**

- `src/infrastructure/connectors/jira/`
- `src/infrastructure/connectors/feishu/`
- `src/workflow/`
- `src/skills/`
- `src/renderers/`
- `src/cli/`
- `tests/integration/`
- `tests/acceptance/`
- `tests/report/`

**步骤：**

1. 让 `preview-write` 输出真实 `rendered_preview` 与结构化 payload。
2. 让 `execute-write` 通过真实 connector 或受控 stub connector 执行真实契约校验，并记录真实 result。
3. 打通统一 `Bugfix Report` 的生成、CLI 查看和 Markdown / JSON 导出。
4. 补齐缺失 skill，或在不新增多余抽象的前提下完成正式命名收敛。

测试/验证：

- 验证对象：真实写回、报告导出与缺失 skill 契约
- 触发方式：覆盖 Jira preview / execute、飞书 preview / execute、dry-run、部分成功、报告导出、skill 契约测试六类场景
- 预期结果：`preview-write` / `execute-write` 能接真实 connector 或 stub connector；`Bugfix Report` 可导出 CLI / Markdown / JSON
- 通过标准：preview / execute、报告导出、skill 契约三条链路都形成正式用户能力

**完成标准：**

- v1 文档中未实现的剩余关键能力全部补齐
- Jira / 飞书真实写回与报告导出成为正式交付项
- v2 收尾后可回写 v1 验收结论

## 6. 建议实施顺序

严格按以下顺序推进，不要为了“看起来快”而跳步：

1. 扩展项目画像与配置检查，支持 Jira 子任务 / branch / commit 绑定规则
2. 扩展核心数据模型、artifact 与 side-effect ledger 契约
3. 新增 CLI 与 workflow 入口，支持 branch 绑定、子任务确保、fix commit 补录
4. 扩展 Jira connector，支持创建子任务与 branch / commit 关联
5. 补齐项目画像与主流程接线
6. 补齐 Jira 真实读取
7. 补齐主流程自动编排
8. 补齐 Requirement Brief 对外交付
9. 补齐代码定位与修复计划主流程接线
10. 补齐子工作流手工补录与 requirement mapping 人工补录
11. 补齐真实写回、报告导出与缺失 skill 契约

## 7. v2 完成标准

当以下条件全部满足时，可认为本轮 v2 目标完成：

- branch 已手工创建后，CLI 能成功绑定到某个 bug Jira
- `ensure-subtask` 在 dry-run 下能生成 preview，在 execute 下能创建 Jira 子任务
- 同一个 bug + branch 重复执行时能 dedupe，不重复建子任务
- `provide-fix-commit` 能把 commit 归属到明确的 bug / subtask
- branch / commit 写回失败后恢复能先 reconcile，不直接重发
- `run start` 能真实走通前五阶段
- `run brief` 能真实生成并导出 `Requirement Brief`
- `preview-write` / `execute-write` 能接真实 connector 或 stub connector
- `record jira` / `record feishu` 能基于最小前置输入独立跑通
- requirement mapping 歧义可人工补录并从 checkpoint 继续
- `Bugfix Report` 可导出 CLI、Markdown、JSON

## 8. 说明

- 本文件是唯一的 v2 执行基线，已直接吸收“Jira 子任务与 GitLab 开发分支绑定、fix commit 显式关联”能力，不再依赖单独扩展计划文件。
- 本次排序将第一优先级固定为前 4 个任务，全部围绕 Jira 子任务、branch、commit 绑定能力展开。
- 本文件只重写 v2 实施计划，不同步改动 v1 需求文档、技术方案和验收文档；待能力真正交付后，再回写对应文档结论。
- 若实施过程中需要改变核心对象、阶段语义、副作用策略或安全策略，必须先按 [AGENTS.md](/Users/sunyi/ai/MySkills/AGENTS.md) 升级确认。
